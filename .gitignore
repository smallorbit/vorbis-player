# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vite
.vite

# Environment variables
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Audio files
public/audio/
.claude/settings.local.json
.claude/HANDOFF.md
.claude/scheduled_tasks.lock
.claude/worktrees/
.claude/retro-notes/

# Electron build outputs
dist-electron/
out/

# SQLite databases
*.db
*.db-journal
*.db-wal

# User data and settings
localLibrarySettings.json
userData/

# Logs
*.log
logs/

# OS generated files
.DS_Store
Thumbs.db

# IDE files
.vscode/settings.json

.env
.cursor/commands/
.cursor/plans/
.certs/

# Conversation exports
it.txt

# Playwright
test-results/

# Capture toolkit
playwright/capture/output/
playwright/capture/.chrome-profile/
# Defensive: legacy real-auth state from the old capture flow (deleted in #1373).
# The current mock-provider capture flow never produces this file, but this
# gitignore guards against accidental token commits if a regression or a
# contributor running an old script ever re-creates it.
playwright/capture/.auth-state.json

# Claude Code sessionkit handoff docs
.sessionkit/

# Local-only stashes of skills moved out of .claude/skills/ so Claude won't load them
.skill-backups/
